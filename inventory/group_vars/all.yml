---
# Static Meraki settings for local development.
# API key: inject via op run --env-file=.env.op (see README).
meraki_organization_id: "383667"

# Admins with null lastActive or lastActive older than this many days are inactive.
inactive_days_threshold: 90
